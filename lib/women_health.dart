import 'package:flutter/material.dart';

class WomenHealth extends StatelessWidget {
  const WomenHealth({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}